# ============================================================
# Security — 绝不能提交密钥 / 私钥
# ============================================================
#
# 签发服务器专属文件 — 这些文件只在内部签发服务器存在，
# 绝对不能进入开源 CE 仓库或客户 EE 部署环境.
#
# git init 之前请先 review 本文件.
#
# ========== 签发服务器专属（不进 CE 仓库） ==========
license_signer.py          # ⚠️ 签发工具：含签发逻辑 + 调用 RSA 私钥
gen_rsa_keys.py            # ⚠️ 密钥对生成脚本
_smoke*.py                 # 签发服务器冒烟测试
_secrets/                  # ⚠️ 签发服务器本地密钥文件

# RSA 密钥文件
*.pem
*.key
*.pfx
*.p12
secrets/

# 签发服务器配置
license_signer_privkey.pem
_signer_config*.py
