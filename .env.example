# ===== Taurus Portal Dev =====
VITE_SITE_NAME=Taurus
VITE_SITE_TITLE=Taurus — 企业级分布式运维平台
VITE_PORT=8082

# ============= 生产部署必填（强烈建议） =============
# 生产站点的绝对源（origin）。用于 SEO：
#   - <link rel=canonical> / <meta og:url> 绝对化
#   - Organization JSON-LD 的 url / logo 绝对化
#   - dist/sitemap.xml 全部 <loc> 前缀 + SPEC 强制 <lastmod> 注入
#   - dist/robots.txt Sitemap: 行绝对化
# 留空时降级为相对路径（本地开发、Staging 可用）。
# 示例 1：VITE_SITE_URL=https://taurus.example.com
# 示例 2：VITE_SITE_URL=https://www.taurus-ops.io
VITE_SITE_URL=

# 后端 API 基地址（Contact 接口默认使用同源反代，可留空）
# 本地分离开发可填绝对 URL，例如：VITE_API_BASE_URL=http://localhost:8000/api
VITE_API_BASE_URL=

# （可选）Contact 表单收件邮箱。不填则使用代码默认值 taurus-stack@outlook.com
# 表单通过 FormSubmit 服务投递到此邮箱，无需自建后端。
# 首次提交后，FormSubmit 会发激活邮件到此地址，点击确认后正式生效。
# 示例：VITE_CONTACT_EMAIL=your-email@example.com
VITE_CONTACT_EMAIL=